import { createSlice } from '@reduxjs/toolkit';

export const carousselSlice = createSlice({
  name: 'caroussel',
  initialState: {
  },
  reducers: {
    
  },
});

// export const { } = carousselSlice.actions;

export default carousselSlice.reducer;

import { createSlice } from '@reduxjs/toolkit';

export const pageNotFoundSlice = createSlice({
  name: 'page-not-found',
  initialState: {
  },
  reducers: {
    
  },
});

// export const { } = pageNotFoundSlice.actions;

export default pageNotFoundSlice.reducer;
